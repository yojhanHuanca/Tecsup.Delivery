CREATE DATABASE IF NOT EXISTS evaluacion_springboot_relaciones;

USE evaluacion_springboot_relaciones;

-- Las tablas se crean automaticamente con Spring Data JPA e Hibernate usando:
-- spring.jpa.hibernate.ddl-auto=update
--
-- Usuarios iniciales cargados por DataLoader:
-- admin / admin123 -> ROLE_ADMIN
-- user  / user123  -> ROLE_USER
